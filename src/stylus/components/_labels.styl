@import '../bootstrap'
@import '../theme'

v-label($material)
  color: $material.text.secondary

theme(v-label, "v-label")

.v-label
  font-size: 16px
  line-height: 1
  transition: .3s $transition.swing
